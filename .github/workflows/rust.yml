name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: rustc -V && cargo build --verbose
    - name: Run tests
      run: rustc -V && cargo test --verbose
